# Static-Site-Generator

A Boot.dev guided project on building a static site generator.

## Notes
Python server: ```python3 -m http.server 8888```

CSS link to HTML: ```<link rel="stylesheet" href="/styles.css" />```

Unittest a directory(src): ```python3 -m unittest discover -s src```